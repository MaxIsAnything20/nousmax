# ---- server-only (never exposed to the browser) ----
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE=
GROQ_API_KEY=
YOUTUBE_API_KEY=

# optional: point the LLM elsewhere (OpenAI-compatible). Defaults to Groq/Llama.
# LLM_BASE_URL=https://api.groq.com/openai/v1
# LLM_MODEL=llama-3.3-70b-versatile

# ---- exposed to the browser (safe; anon key is public, RLS protects data) ----
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
